services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: elawyer
      POSTGRES_USER: elawyer
      POSTGRES_PASSWORD: elawyer
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:

